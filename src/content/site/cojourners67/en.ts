import type { SiteContentMap } from 'src/types/SiteContent';

const sharedEn: SiteContentMap = {
  review: {
    paras: ['Fallback review paragraph from shared site content.'],
    empty: 'Fallback empty review message.',
  },
};

export default sharedEn;
